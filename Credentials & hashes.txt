Credentials & hashes (copy these exactly)

Ghost (super admin)

Username: Ghost

Plain password: Super!Pass2025#

Bcrypt hash:

$2b$12$gynOMrKXKvN7eoCM5IqQEur/cCYzjjbvknr1Z90iv4xDZ38X0duwy


Admin (if your username is admin)

Username: Kaigu

Plain password: Admin!Pass2025#

Bcrypt hash:

$2b$12$3mVpuIpFjyN.gmK9XEjr4.3W.19k5KqHWoP7XiA/9z.ol6XBP1Vdm



<?php
$host = "172.16.0.108";
$port = 3307;
$username = "admin";
$password = "admin1234";
$database = "contact";

$conn = new mysqli($host, $username, $password, $database, $port);

// Check connection
if ($conn->connect_error) {
    // Log the detailed error (to a file, not shown to user)
    error_log("DB Connection failed: " . $conn->connect_error);
    // Show a generic safe message
    die("Database connection error. Please try again later.");
}

// Set UTF-8 charset
if (!$conn->set_charset("utf8mb4")) {
    error_log("Error loading character set utf8mb4: " . $conn->error);
    die("Database configuration error.");
}
?>
